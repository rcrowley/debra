prefix=@prefix@
bindir=@bindir@

all:
	@true
 
install:
	mkdir -p $(DESTDIR)$(bindir)
	install -c bin/sourceinstall $(DESTDIR)$(bindir)/
	install -c bin/debra $(DESTDIR)$(bindir)/
 
uninstall:
	rm $(DESTDIR)$(bindir)/sourceinstall $(DESTDIR)$(bindir)/debra
