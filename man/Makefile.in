ron=@ron@
gzip=@gzip@

RON=$(wildcard *.md)
ROFF=$(RON:.md=)

all: clean $(ROFF)

clean:
	rm -f $(ROFF)

%.1 %.2 %.3 %.4 %.5 %.6 %.7 %.8:
ifneq (, $(ron))
	$(ron) -b $@.md
	chmod 644 $@
ifneq (, $(gzip))
	$(gzip) -f $@
endif
endif

.PHONY: all clean
